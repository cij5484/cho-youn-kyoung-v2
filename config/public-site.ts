// Prepared artifact only. DNS and GitHub Pages domain settings are separate manual actions.
export const productionSite = {
  origin: 'https://choyounkyoung.com',
  base: '/',
  directory: 'build-production',
  port: 4185,
} as const
